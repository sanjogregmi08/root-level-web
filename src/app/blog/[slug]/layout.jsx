export const metadata = {
  template: {
    title: "Blog | Root Level",
    default: "Blog | Root Level",
  },
  description: "Details of the blog post",
};

export default function BlogSlugLayout({ children }) {
  return <>{children}</>;
}
